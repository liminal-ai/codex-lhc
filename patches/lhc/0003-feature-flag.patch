diff --git a/codex-rs/core/config.schema.json b/codex-rs/core/config.schema.json
index 05528ce3f1..3292ddcf97 100644
--- a/codex-rs/core/config.schema.json
+++ b/codex-rs/core/config.schema.json
@@ -609,6 +609,9 @@
             "js_repl_tools_only": {
               "type": "boolean"
             },
+            "lhc_capture": {
+              "type": "boolean"
+            },
             "local_thread_store_compression": {
               "type": "boolean"
             },
@@ -5251,6 +5254,9 @@
         "js_repl_tools_only": {
           "type": "boolean"
         },
+        "lhc_capture": {
+          "type": "boolean"
+        },
         "local_thread_store_compression": {
           "type": "boolean"
         },
diff --git a/codex-rs/features/src/lib.rs b/codex-rs/features/src/lib.rs
index 31cb6afe37..a9159172c7 100644
--- a/codex-rs/features/src/lib.rs
+++ b/codex-rs/features/src/lib.rs
@@ -279,6 +279,9 @@ pub enum Feature {
     PreventIdleSleep,
     /// Enable remote compaction v2 over the normal Responses API.
     RemoteCompactionV2,
+    /// Enable LHC long-horizon capture (parallel SQLite record; product default on).
+    // LHC-HOOK 3/8: feature flag for LHC capture (rollback = flag off).
+    LhcCapture,
     /// Retain client-authored developer messages across compacted context windows.
     RetainClientDeveloperMessages,
     /// Use Agent Identity for ChatGPT-authenticated sessions.
@@ -1456,6 +1459,13 @@ pub const FEATURES: &[FeatureSpec] = &[
         stage: Stage::Stable,
         default_enabled: true,
     },
+    // LHC-HOOK 3/8: FeatureSpec for LhcCapture (default ON in the product fork).
+    FeatureSpec {
+        id: Feature::LhcCapture,
+        key: "lhc_capture",
+        stage: Stage::Stable,
+        default_enabled: true,
+    },
     FeatureSpec {
         id: Feature::RetainClientDeveloperMessages,
         key: "retain_client_developer_messages",
