diff --git a/codex-rs/core/config.schema.json b/codex-rs/core/config.schema.json
index b6a078b3da..3ea56a2ee1 100644
--- a/codex-rs/core/config.schema.json
+++ b/codex-rs/core/config.schema.json
@@ -769,6 +769,9 @@
             "js_repl_tools_only": {
               "type": "boolean"
             },
+            "lhc_capture": {
+              "type": "boolean"
+            },
             "local_thread_store_compression": {
               "type": "boolean"
             },
@@ -1788,9 +1791,81 @@
       ],
       "description": "One action binding value in config.\n\nThis accepts either:\n\n1. A single key or chord string (`\"ctrl-a\"` or `\"ctrl-x ctrl-s\"`). 2. A list of alternative bindings (`[\"ctrl-a\", \"ctrl-x ctrl-s\"]`).\n\nAn empty list explicitly unbinds the action in that scope. Because an explicit empty list is still a configured value, runtime resolution must not fall through to global or built-in defaults for that action."
     },
+    "LcAdaptiveServiceTierConfig": {
+      "additionalProperties": false,
+      "description": "Cost control for long-context agents that normally use Fast service.\n\nBelow `threshold`, requests use `below`. At or above it, requests use `at_or_above`, avoiding simultaneous Fast and long-context multipliers. Compaction naturally restores the lower-context tier on the next request.",
+      "properties": {
+        "at_or_above": {
+          "default": "default",
+          "type": "string"
+        },
+        "below": {
+          "default": "fast",
+          "type": "string"
+        },
+        "enabled": {
+          "default": false,
+          "type": "boolean"
+        },
+        "threshold": {
+          "default": 272000,
+          "format": "int64",
+          "type": "integer"
+        }
+      },
+      "type": "object"
+    },
     "LegacyAppPathString": {
       "type": "string"
     },
+    "LhcCompactConfig": {
+      "additionalProperties": false,
+      "properties": {
+        "percentages": {
+          "allOf": [
+            {
+              "$ref": "#/definitions/LhcCompactPercentages"
+            }
+          ],
+          "default": {
+            "brief": 25.0,
+            "detailed": 25.0,
+            "full": 25.0,
+            "smooth": 25.0
+          }
+        }
+      },
+      "type": "object"
+    },
+    "LhcCompactPercentages": {
+      "additionalProperties": false,
+      "description": "Per-session LHC compact band allocation.",
+      "properties": {
+        "brief": {
+          "format": "double",
+          "type": "number"
+        },
+        "detailed": {
+          "format": "double",
+          "type": "number"
+        },
+        "full": {
+          "format": "double",
+          "type": "number"
+        },
+        "smooth": {
+          "format": "double",
+          "type": "number"
+        }
+      },
+      "required": [
+        "brief",
+        "detailed",
+        "full",
+        "smooth"
+      ],
+      "type": "object"
+    },
     "MarketplaceConfig": {
       "additionalProperties": false,
       "properties": {
@@ -5775,6 +5850,9 @@
         "js_repl_tools_only": {
           "type": "boolean"
         },
+        "lhc_capture": {
+          "type": "boolean"
+        },
         "local_thread_store_compression": {
           "type": "boolean"
         },
@@ -6087,6 +6165,22 @@
       "description": "System instructions.",
       "type": "string"
     },
+    "lc_adaptive_service_tier": {
+      "allOf": [
+        {
+          "$ref": "#/definitions/LcAdaptiveServiceTierConfig"
+        }
+      ],
+      "description": "Optional LC Adaptive Service Tier cost control. When enabled, this owns request tier selection using the prepared long-context request size."
+    },
+    "lhc_compact": {
+      "allOf": [
+        {
+          "$ref": "#/definitions/LhcCompactConfig"
+        }
+      ],
+      "description": "Per-session LHC compact band allocation."
+    },
     "log_dir": {
       "allOf": [
         {
diff --git a/codex-rs/features/src/lib.rs b/codex-rs/features/src/lib.rs
index a739f34c98..f3c5240ccf 100644
--- a/codex-rs/features/src/lib.rs
+++ b/codex-rs/features/src/lib.rs
@@ -313,6 +313,9 @@ pub enum Feature {
     PreventIdleSleep,
     /// Enable remote compaction v2 over the normal Responses API.
     RemoteCompactionV2,
+    /// Enable LHC long-horizon capture (parallel SQLite record; product default on).
+    // LHC-HOOK 3/8: feature flag for LHC capture (rollback = flag off).
+    LhcCapture,
     /// Include retained images in the remote compaction context budget.
     CompactionImageBudget,
     /// Retain client-authored developer messages across compacted context windows.
@@ -1573,6 +1576,19 @@ pub const FEATURES: &[FeatureSpec] = &[
         stage: Stage::Stable,
         default_enabled: true,
     },
+    // LHC-HOOK 3/8: FeatureSpec for LhcCapture (default ON in the product fork).
+    FeatureSpec {
+        id: Feature::LhcCapture,
+        key: "lhc_capture",
+        stage: Stage::Stable,
+        default_enabled: true,
+    },
+    FeatureSpec {
+        id: Feature::CompactionImageBudget,
+        key: "compaction_image_budget",
+        stage: Stage::UnderDevelopment,
+        default_enabled: false,
+    },
     FeatureSpec {
         id: Feature::CompactionImageBudget,
         key: "compaction_image_budget",
